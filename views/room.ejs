<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <title>Chat App</title>
  <script>
    const roomName = "<%= roomName %>"
  </script>
  <script defer src="http://localhost:3000/socket.io/socket.io.js"></script>
  <script defer src="handler.js"></script>
  <style>
    /* width */
::-webkit-scrollbar {
  width: 10px;
}

/* Track */
::-webkit-scrollbar-track {
  box-shadow: inset 0 0 5px grey;
  border-radius: 10px;
}

/* Handle */
::-webkit-scrollbar-thumb {
  background: rgb(0, 37, 20);
  border-radius: 10px;
}

    body {
      padding: 0;
      margin: 0;
      display: flex;
      justify-content: center;
      background-image: url("back.jpg");
      background: linear-gradient( rgba(0, 0, 0, 0.5), rgba(0, 0, 0, 0.5) ), url("back.jpg");
      background-size: cover;
    }

    #msg_container div {
      background-color: #0000007a;
      color: whitesmoke;
      padding: 9px;
      border-radius: 5px;
      margin-bottom: 20px;
      font-family: Segoe UI;
    }

    #msg_container div:hover{
      background: linear-gradient( rgba(0, 0, 0, 0.5), rgba(0, 0, 0, 0.5) ), url("back.jpg");
    }

    #msg_container {
      width: 60%;
      max-width: 1200px;
      margin-top: 100px;
      height: 400px;
      overflow-y: scroll;
      padding: 0px 40px;
    }

    #send_cont {
      position: fixed;
      bottom: 0;
      /* background-color: rgb(255, 255, 255); */
      max-width: 1200px;
      width: 60%;
      display: flex;
      height: 45px;
      margin-bottom: 30px;
    }

    /* #msg_container div:nth-child(2n) {
      border-radius: 9px;
      background-color: #e6e3e3;
      margin-top: 20px;
    } */

    #msg_input {
      flex-grow: 1;
      /* width: 50%; */
      height: 70%;
      background: rgba(0, 0, 0, 0.568);
      color: whitesmoke;
      border: solid 1px rgba(245, 245, 245, 0.432);
      border-radius: 30px;
      margin: auto;
      /* margin-left: 20px; */
      margin-right: 20px;
      padding: 0px 10px;
    }

    #send-button {
      /* color: #fff !important; */
      text-transform: uppercase;
      text-decoration: none;
      background: rgba(0, 185, 71, 0.726);
      color: black;
      /* padding: 20px; */
      display: inline-block;
      border: none;
      width: 15%;
      height: 80%;
      margin: auto;
      border-radius: 30px;
      font-weight: bold;
    }
    #send-button:hover{
      background: black;
      color: rgba(0, 185, 71, 0.726);
      border: solid 1px rgba(0, 185, 71, 0.726);
      cursor: pointer;
    }
    by {
      color: rgb(0, 185, 71);
      font-weight: bold;
    }
    input:focus {
    outline-width: 0;
    }
    .cap{
      text-transform: uppercase;
    }
  </style>
</head>

<body>
  <div id="msg_container">
    <div><center>Welcome to the<by class="cap"> <%= roomName %></by> chatroom.</center></div>
  </div>
  <form id="send_cont">t
    <input type="text" id="msg_input">
    <button type="submit" id="send-button">Send</button>
  </form>
</body>

</html>